node_modules/

# Ignore gzip files
*.gz

# Ignore Next.js build output
.next/

# Ignore Next.js cache files
.next/cache/
.next/build-manifest.json
.next/prerender-manifest.json
.next/routes-manifest.json
.next/trace
.next/types/
docker/*.tar
# Backend image archive (loaded via `docker load`, not source) — keep out of git
*.tar

# Local backend secrets (committed template: docker/.env.example)
docker/.env
.env
.env.*
!.env.example
.vercel

# Personal / machine-local
.claude/settings.local.json
.DS_Store
.prod-backups/

# Two-agent AI pipeline working files — kept out of the committed paper trail.
.ai/scratch/
.ai/runs/
.ai/transcripts/
.ai/prompts/

# Build and coverage
dist/
build/
coverage/

# Python caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
__pycache__/

# Playwright test artifacts
playwright-report/
test-results/
